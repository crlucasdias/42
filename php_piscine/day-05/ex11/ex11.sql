SELECT UPPER(user_card.last_name) as "NAME", user_card.first_name, subscription.price from user_card  INNER JOIN member ON user_card.id_user = member.id_user_card INNER JOIN subscription USING(id_sub) WHERE subscription.price > 42 ORDER BY user_card.last_name, user_card.first_name ASC;
